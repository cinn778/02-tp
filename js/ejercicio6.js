 

let filas="30"
let columnas= "30"

for (filas = 1; filas <= 30; filas++) {
    
    for (columnas = 0; columnas < filas; columnas++) {
       
        document.write(filas);
    }

    document.write("<br>");
}
